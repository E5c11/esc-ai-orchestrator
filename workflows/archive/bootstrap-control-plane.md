# Bootstrap Control Plane
**Status:** Complete

## Delivered

- SQLite task, run, and event persistence
- In-process queue and worker lifecycle
- Replaceable runtime protocol
- OpenCode runtime consuming framework contracts
- Task submission and task/run/event observation HTTP API
- Fake-runtime scheduler and end-to-end HTTP tests

## Deferred

- Authentication and multi-user authorization
- PostgreSQL and distributed queues/workers
- Remote workspace lifecycle
- Approval endpoints and streaming WebSockets
- Retry, cancellation, checkpoint/resume scheduling
- Production deployment and observability
